# Build directories
/build/
/out/
cmake-build-*/
*.o
*.obj
*.a
*.so
*.so.*
*.dylib
*.dll

# Compiled shaders
*.spv

# Rust
/target/
**/target/
Cargo.lock

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Test / debug artifacts
/tests/artifacts/
*.png.actual
flow_*.png
!docs/**/*.png

# debug flow dumps
*.ppm
flow.png

# CUDA build dir
/build-cuda/

# vendored NGX SDK (binaries kept out of git; license + headers only)

# vendored NVIDIA NGX SDK (local only; NVIDIA-licensed, not redistributed)
renderfx/vendor/ngx/
/build-ngx/
renderfx/vendor/xess/

# Path B (native DLSS-G loader) — interoperability research, TRACKED explicitly.
# Only NVIDIA's own binaries and the private RE notebook stay out (see also
# experiments/dlssg-native/.gitignore). Never commit *.dll / *.exe blobs.

# Plan-A OWN MODEL — private weights, checkpoints, training data, run dirs.
# Never commit: our trained model is private IP. Code (*.py, *.cpp, *.cu) stays tracked;
# only artefacts below are ignored.
*.nvfgw
*.pt
*.pth
*.ckpt
*.safetensors
*.onnx
latest.pt
best.pt
ckpt_*.pt
checkpoints/
**/checkpoints/
runs/
training/runs/
plana_runs/
~/plana_runs/

# Training data (A2) + golden blobs
*.npz
frame_*.npz
clip_*/
gold*.nvfgw
gold_*.bin
*_x.bin
*_y.bin
fg_v1*.nvfgw

# Run control / logs
train.log
console.log
PAUSE
args

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Local user cache / probe logs (never commit)
nvsdk_ngx.log
renderfx/vendor/ngx/lib/Linux_x86_64/rel/nvsdk_ngx.log
